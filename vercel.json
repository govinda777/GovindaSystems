{
  "buildCommand": "cd src/web && yarn build",
  "outputDirectory": "src/web/.next",
  "installCommand": "cd src/web && yarn install",
  "framework": "nextjs",
  "rewrites": [
    { "source": "/(.*)", "destination": "/src/web/$1" }
  ]
}
